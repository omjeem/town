name: Build Docker Images

# Release tag conventions:
#   - Bare semver (e.g. `0.1.0`) → builds redplanethq/town:0.1.0
# Each tag triggers exactly one image.

on:
  push:
    tags:
      - "*"
  workflow_dispatch:

jobs:
  build-webapp:
    runs-on: [self-hosted, linux, x64, aws, spot]

    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Registry
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: Build and Push Web Docker Image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./hosting/web/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: redplanethq/town:${{ github.ref_name }}
          cache-from: type=registry,ref=redplanethq/town:buildcache
          cache-to: type=registry,ref=redplanethq/town:buildcache,mode=max
